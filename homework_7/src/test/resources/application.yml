spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: test-data.sql

  shell:
    interactive:
      enabled: false

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      show-sql: false
